auth:
  clients:
    dontgoback-core-server: ${AUTH_CLIENTS_DONTGOBACK_CORE_SERVER}
    dontgoback-extension-server: ${AUTH_CLIENTS_DONTGOBACK_EXTENSION_SERVER}

  jwt:
    issuer: ${AUTH_JWT_ISSUER}

  key:
    private-key-path: ${AUTH_KEY_PRIVATE_KEY_PATH}
    public-key-path: ${AUTH_KEY_PUBLIC_KEY_PATH}
